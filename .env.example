# Local Postgres for now (see docs/schema/README.md). When ready to move
# to a hosted Supabase project, this is the only line that changes —
# schema.sql runs on vanilla Postgres either way.
DATABASE_URL=postgres://localhost:5432/re_os_dev
